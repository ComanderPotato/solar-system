{
  "name": "solar-system",
  "version": "1.0.0",
  "scripts": {
    "deploy": "uvicorn backend.main:app --host 0.0.0.0 --port 80",
    "build": "cd frontend && npm run build",
    "start": "npm run build && fastapi dev backend/main.py",
    "dev": "concurrently \"cd frontend && npm run build\" \"cd backend && fastapi dev main.py\"",
    "server": "uvicorn backend.main:app --reload --port 8000",
    "setup": "node setup.mjs"
  },
  "devDependencies": {
    "concurrently": "^8.0.0"
  }
}
