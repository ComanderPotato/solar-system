import OrbitingBody from "../../models/OrbitingBody";

export default interface IOrbitingBodyController {
	// updateVelocity(body: OrbitingBody, dt: number, other?: OrbitingBody): void;
	// updateOrbitingBodyPosition(body: OrbitingBody, dt: number): void;
	// calculcateOrbitingBodyAcceleration(body: OrbitingBody): void;
	// drawOrbit(body: OrbitingBody): void;
	// initialiseOrbit(body: OrbitingBody): void;
	// initialiseOrbitalPlane(body: OrbitingBody): void;
}
